<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/bodyBackground"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.henrymatidios.wayngalan.AccountsActivity">

    <ScrollView
        android:id="@+id/account_page"
        android:layout_width="336dp"
        android:layout_height="463dp">

        <LinearLayout
            android:id="@+id/account_page_form"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="@dimen/loginPageTopMargin"
            android:orientation="vertical">

            <Button
                android:id="@+id/view_user_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/button_marginTop"
                android:background="@color/buttonColor"
                android:text="@string/action_view_user"
                android:textColor="@color/textColor"
                android:textSize="@dimen/font_size" />

            <Button
                android:id="@+id/add_new_user_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/button_marginTop"
                android:background="@color/buttonColor"
                android:text="@string/action_add_new_user"
                android:textColor="@color/textColor"
                android:textSize="@dimen/font_size" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>
