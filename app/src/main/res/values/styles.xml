<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorHint">@color/textColor</item>
        <item name="android:colorBackground">@color/bodyBackground</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="spinnerItemStyle" parent="android:Widget.Material.Spinner">
        <item name="android:textColor">@color/textColor</item>
    </style>
    <style name="spinnerDropDownStyle" parent = "android:Widget.Material.DropDownItem.Spinner">
        <item name="android:textColor">@color/textColor</item>
    </style>
</resources>
